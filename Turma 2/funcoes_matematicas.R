#install.packages("forecast")

library(forecast)

seno10 <- sin(10)
coseno <- cos(180)
tangente <- tan(90)
raiz1 <- sqrt(4)
raiz2 <- sqrt(10)
logaritmo1 <- log(8, base = 4)
logaritmo2 <- log(10, base = 10)

